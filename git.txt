cd
dir 
cd ..
cls 

per project one time setup

1. > git init
2. > git remote add origin <gitUrl>
    example: git remote add origin https://github.com/sagartmg2/falgun

    (optional)
    > git remote -v 


every day 


1. > git status (optional)
2. add 
       >  git add .  // all at once
       > git add <filePath> // one at a time
3. > git commit -m "message"
4. > git push



research on 
    git fork (optinal)
    git branches
    git pull request


clone
pull