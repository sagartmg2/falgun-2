cd
dir 
cd ..
cls 

per project one time setup

1. > git init
2. > git remote add origin <gitUrl>
    example: git remote add origin https://github.com/sagartmg2/falgun-2

    > git remote -v (optional)


every day 

1. > git status
2. > git add .
3. > git commit -m "message"
